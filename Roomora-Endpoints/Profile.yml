info:
  name: Profile
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/api/v1/profile
  headers:
    - name: X-Dev-Clerk-Id
      value: dev_student_1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
